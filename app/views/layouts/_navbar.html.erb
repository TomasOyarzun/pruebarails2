<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#"> PRUEBA RAILS 2 </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
   <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <%if user_signed_in?%>
      <li class="nav-item active">
        <a class="nav-link" href="<%= destroy_user_session_path%>" data-method="DELETE"> Salir </a>
      </li>
      <h1> TAREAS REALIZADAS: <%= current_user.tasks.count %> / <%=Task.count %></h1>
      <% else %>
          <li class="nav-item active"><a class="nav-link" href="<%= new_user_session_path%>"> Logearse </a></li>
          <li class="nav-item"><a class="nav-link" href="<%= new_user_registration_path%>"> Registrarse </a></li>
      <% end %>
    </ul>
  </div>
</nav>